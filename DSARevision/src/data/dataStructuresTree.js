import { JAVA_SNIPPETS } from './javaSnippets.js';

export const DATA_STRUCTURE_TREE = {
  id: "ds-root",
  icon: "🧱",
  question: "Pick a data structure to revise",
  options: [
    {
      label: "ArrayList",
      result: {
        name: "ArrayList",
        pkg: "java.util.ArrayList",
        description: "ArrayList is a resizable array implementation backed by a plain Object[] array. When the internal array fills up, it grows by ~50% automatically. It stores elements in contiguous memory, giving O(1) random access by index and excellent CPU cache performance. Because it is ordered and index-addressable, it is the default go-to list when element order and fast reads matter more than frequent mid-list modifications.\n\nAdvantages: O(1) get/set by index, amortized O(1) append, compact memory layout, great cache locality.\nDisadvantages: O(n) insert/delete in the middle (elements must shift), occasional O(n) resize copy, not thread-safe.",
        whenToUse: "Use when reads by index dominate and inserts are mostly at the end.",
        why: "Contiguous memory gives excellent cache locality and O(1) index access.",
        complexity: { get: "O(1)", append: "O(1) amortized", insertMid: "O(n)", removeMid: "O(n)" },
        tradeoffs: "Middle inserts/deletes shift elements; pre-size if capacity growth is predictable.",
        javaCode: JAVA_SNIPPETS.arrayList,
        color: "#22c55e",
      },
    },
    {
      label: "LinkedList (Deque API)",
      result: {
        name: "LinkedList (Deque API)",
        pkg: "java.util.LinkedList",
        description: "LinkedList is a doubly-linked list where each node holds a value plus forward and backward pointers. Java's LinkedList also implements the Deque interface, making it usable as both a stack and a queue. Because no shifting is needed, adding or removing at either end is O(1). However, pointer-chasing for random access means O(n) indexed reads and poor cache performance compared to ArrayList.\n\nAdvantages: O(1) add/remove at head or tail, natural Deque/Queue/Stack API, no capacity pre-allocation needed.\nDisadvantages: O(n) random access, extra memory per node (two pointers), poor cache locality, usually slower than ArrayDeque for pure stack/queue use.",
        whenToUse: "Use when head/tail updates are far more common than random indexing.",
        why: "Doubly linked nodes allow O(1) endpoint operations.",
        complexity: { addFirst: "O(1)", addLast: "O(1)", getIndex: "O(n)", removeMiddle: "O(n)" },
        tradeoffs: "Poor locality and extra memory per node; often slower than ArrayDeque in practice.",
        javaCode: JAVA_SNIPPETS.linkedList,
        color: "#16a34a",
      },
    },
    {
      label: "Array (int[] / T[])",
      result: {
        name: "Array (int[] / T[])",
        pkg: "built-in",
        description: "A primitive Java array is a fixed-size block of contiguous memory. For int[], long[], char[], etc., values are stored directly (no boxing), making it the most memory-efficient and cache-friendly collection. Size is set at creation and cannot change; you must allocate a new array and copy if more space is needed. It underpins virtually every other data structure.\n\nAdvantages: O(1) read/write by index, lowest memory overhead (primitives stored inline), no boxing/unboxing cost, fastest iteration.\nDisadvantages: Fixed size — no dynamic growth, no built-in add/remove helpers, manual bounds management.",
        whenToUse: "Use when size is known and stable, especially for primitives.",
        why: "Lowest overhead and direct memory layout.",
        complexity: { readWriteIndex: "O(1)", search: "O(n)", binarySearchSorted: "O(log n)" },
        tradeoffs: "No dynamic resizing; manual copy needed for growth.",
        javaCode: `int[] a = new int[5];
a[0] = 10;
int x = a[0];
Arrays.sort(a);
int pos = Arrays.binarySearch(a, 10);`,
        color: "#65a30d",
      },
    },
    {
      label: "HashMap",
      result: {
        name: "HashMap",
        pkg: "java.util.HashMap",
        description: "HashMap stores key-value pairs in an array of buckets. A hash function maps each key to a bucket index; collisions are resolved by a linked list or (when a bucket exceeds 8 entries) a balanced tree (Java 8+). The load factor (default 0.75) triggers a resize-and-rehash when the table gets too full. This gives average O(1) get/put/remove but O(n) worst-case on pathological hash collisions.\n\nAdvantages: Average O(1) for all core operations, simple API, works with any key that has a proper hashCode/equals.\nDisadvantages: No guaranteed iteration order, worst-case O(n) on bad hashes, not thread-safe, slight overhead from boxing primitives.",
        whenToUse: "Use for standard dictionary/frequency tasks with average O(1) operations.",
        why: "Hash buckets make get/put/remove very fast with good hash distribution.",
        complexity: { get: "O(1) avg", put: "O(1) avg", remove: "O(1) avg", iterate: "O(n)" },
        tradeoffs: "No deterministic order; poor hashCode can degrade performance.",
        codeVariants: [
          { title: "Java usage", code: JAVA_SNIPPETS.hashMap },
          { title: "How HashMap works (Java internals)", code: JAVA_SNIPPETS.hashMapInternals },
        ],
        javaCode: JAVA_SNIPPETS.hashMap,
        color: "#3b82f6",
      },
    },
    {
      label: "LinkedHashMap",
      result: {
        name: "LinkedHashMap",
        pkg: "java.util.LinkedHashMap",
        description: "LinkedHashMap extends HashMap by weaving all entries into a doubly-linked list in insertion order (or access order when constructed with accessOrder=true). This extra list has negligible overhead per entry but guarantees predictable, deterministic iteration — critical for LRU caches where the eviction candidate is always the least-recently-accessed entry at the tail of the list.\n\nAdvantages: Same O(1) average ops as HashMap, deterministic insertion/access-order iteration, built-in LRU cache support via removeEldestEntry.\nDisadvantages: Slightly higher memory than plain HashMap due to two extra pointers per entry; not thread-safe.",
        whenToUse: "Use when hash-map speed is needed and iteration must follow insertion/access order.",
        why: "Hash table + doubly linked entry list preserves deterministic order.",
        complexity: { get: "O(1) avg", put: "O(1) avg", remove: "O(1) avg", iterateOrder: "O(n)" },
        tradeoffs: "Extra pointer overhead vs HashMap.",
        javaCode: JAVA_SNIPPETS.linkedHashMap,
        color: "#0ea5e9",
      },
    },
    {
      label: "TreeMap",
      result: {
        name: "TreeMap",
        pkg: "java.util.TreeMap",
        description: "TreeMap is a Red-Black Tree backed sorted map. Keys are kept in natural order (or a supplied Comparator order) at all times, so you can navigate the map with floor, ceiling, lower, higher, headMap, tailMap, and subMap in O(log n). All structural operations (get/put/remove) are O(log n). It is the Java standard-library choice whenever you need both a key-value map and order-aware queries.\n\nAdvantages: Guaranteed O(log n) operations, rich NavigableMap API, always-sorted iteration, no hashCode requirement on keys.\nDisadvantages: ~3-5x slower than HashMap for plain lookup, more memory per entry (node + color bit + parent/child pointers), not thread-safe.",
        whenToUse: "Use when floor/ceiling/range queries or sorted iteration is required.",
        why: "Balanced tree keeps keys ordered and supports navigable operations.",
        complexity: { get: "O(log n)", put: "O(log n)", floorKey: "O(log n)", iterateSorted: "O(n)" },
        tradeoffs: "Slower constants than HashMap for plain lookup.",
        javaCode: JAVA_SNIPPETS.treeMap,
        color: "#ef4444",
      },
    },
    {
      label: "HashSet",
      result: {
        name: "HashSet",
        pkg: "java.util.HashSet",
        description: "HashSet is simply a HashMap where only keys are stored (values are a dummy constant). It inherits all the hashing internals: bucket array, separate chaining, load-factor-driven resize. Contains/add/remove are average O(1). Iteration order is undefined and changes after rehash.\n\nAdvantages: Average O(1) for contains/add/remove, minimal API surface, ideal for deduplication and visited-marking.\nDisadvantages: No guaranteed order, not thread-safe, boxing overhead for primitive types, O(n) worst-case on hash collision.",
        whenToUse: "Use for deduplication and membership checks where order is irrelevant.",
        why: "Backed by hash table for average O(1) contains/add/remove.",
        complexity: { add: "O(1) avg", contains: "O(1) avg", remove: "O(1) avg", iterate: "O(n)" },
        tradeoffs: "No stable order.",
        javaCode: JAVA_SNIPPETS.hashSet,
        color: "#2563eb",
      },
    },
    {
      label: "LinkedHashSet",
      result: {
        name: "LinkedHashSet",
        pkg: "java.util.LinkedHashSet",
        description: "LinkedHashSet wraps a LinkedHashMap (keys only) so it combines the O(1) average membership operations of a hash set with a predictable, insertion-order iteration sequence. The internal doubly-linked list connects entries in the order they were first inserted, and that order persists through removes.\n\nAdvantages: O(1) average contains/add/remove, deterministic insertion-order iteration, clean deduplication with order preservation.\nDisadvantages: Slightly higher memory than HashSet (two extra pointers per entry), not thread-safe, no sorted order.",
        whenToUse: "Use when uniqueness and insertion-order iteration both matter.",
        why: "Hash-based set with linked iteration order.",
        complexity: { add: "O(1) avg", contains: "O(1) avg", iterateInOrder: "O(n)" },
        tradeoffs: "Slightly more memory than HashSet.",
        javaCode: JAVA_SNIPPETS.linkedHashSet,
        color: "#0891b2",
      },
    },
    {
      label: "TreeSet",
      result: {
        name: "TreeSet",
        pkg: "java.util.TreeSet",
        description: "TreeSet is a TreeMap with only keys (values are a dummy constant). The underlying Red-Black Tree keeps all elements in sorted order at all times. It implements NavigableSet, giving you floor, ceiling, lower, higher, headSet, tailSet, and subSet in O(log n). Perfect when you need a dynamically maintained sorted unique collection with predecessor/successor lookups.\n\nAdvantages: Always sorted, O(log n) add/remove/contains, full NavigableSet API (floor, ceiling, range views), no hashCode required.\nDisadvantages: O(log n) is slower than hash-based O(1), higher memory per element, not thread-safe, no duplicate support (use TreeMap with counts for multisets).",
        whenToUse: "Use when you need sorted unique elements and nearest-neighbor queries.",
        why: "Balanced tree supports sorted operations efficiently.",
        complexity: { add: "O(log n)", contains: "O(log n)", ceiling: "O(log n)", iterateSorted: "O(n)" },
        tradeoffs: "Higher operation cost than hash-based sets.",
        javaCode: JAVA_SNIPPETS.treeSet,
        color: "#dc2626",
      },
    },
    {
      label: "Stack",
      result: {
        name: "Stack",
        pkg: "ArrayDeque / Stack / LinkedList / Custom Array",
        description: "A Stack is a Last-In-First-Out (LIFO) abstract data structure: the most recently pushed element is always the first to be popped. Java's preferred implementation is ArrayDeque, which uses a growable circular array and avoids synchronization overhead. Stacks naturally model nested structure: function call frames, matching brackets, DFS recursion, and undo histories.\n\nAdvantages: O(1) push/pop/peek, simple mental model, eliminates explicit recursion in DFS, essential for monotonic-stack patterns.\nDisadvantages: No random access, only the top is visible; the legacy java.util.Stack class is synchronized and should be avoided in modern code.",
        whenToUse: "Use for LIFO workflows: DFS, expression parsing, monotonic stack, undo/backtracking.",
        why: "Last pushed item is popped first, which naturally models nested decisions and reversals.",
        complexity: { push: "O(1) amortized", pop: "O(1)", peek: "O(1)" },
        tradeoffs: "Prefer ArrayDeque in modern Java; legacy Stack is synchronized and usually slower.",
        codeVariants: [
          { title: "Recommended (ArrayDeque)", code: JAVA_SNIPPETS.arrayDequeStack },
          { title: "All Java ways", code: JAVA_SNIPPETS.stackWaysJava },
          { title: "From scratch implementation", code: JAVA_SNIPPETS.stackFromScratch },
        ],
        javaCode: JAVA_SNIPPETS.arrayDequeStack,
        color: "#f59e0b",
      },
    },
    {
      label: "Queue",
      result: {
        name: "Queue",
        pkg: "ArrayDeque / LinkedList / PriorityQueue / Custom Circular Array",
        description: "A Queue is a First-In-First-Out (FIFO) abstract data structure: elements are added at the tail and removed from the head. ArrayDeque is the recommended implementation — it uses a resizable circular array with O(1) amortized offer/poll/peek. Queues are the backbone of BFS (level-order graph traversal), producer-consumer pipelines, and round-robin scheduling.\n\nAdvantages: O(1) enqueue/dequeue/peek, level-by-level BFS processing naturally encoded, simple producer-consumer model.\nDisadvantages: No random access or index lookup, not thread-safe (use LinkedBlockingQueue for concurrent use), PriorityQueue serves different semantics and is NOT FIFO.",
        whenToUse: "Use for FIFO workflows: BFS levels, task scheduling, buffering, and event pipelines.",
        why: "First inserted element leaves first, matching arrival-order processing.",
        complexity: { offer: "O(1) amortized", poll: "O(1)", peek: "O(1)" },
        tradeoffs: "Use ArrayDeque for normal FIFO; PriorityQueue is not FIFO and serves different semantics.",
        codeVariants: [
          { title: "Recommended (ArrayDeque)", code: JAVA_SNIPPETS.arrayDequeQueue },
          { title: "All Java ways", code: JAVA_SNIPPETS.queueWaysJava },
          { title: "From scratch implementation", code: JAVA_SNIPPETS.queueFromScratch },
        ],
        javaCode: JAVA_SNIPPETS.arrayDequeQueue,
        color: "#f59e0b",
      },
    },
    {
      label: "PriorityQueue",
      result: {
        name: "PriorityQueue",
        pkg: "java.util.PriorityQueue",
        description: "PriorityQueue implements a binary heap: a complete binary tree stored in an array where every parent is smaller (min-heap, the default) or larger (max-heap with reverseOrder()) than its children. The heap property ensures the extreme element is always at the root (index 0), enabling O(1) peek and O(log n) offer/poll. It does NOT guarantee FIFO among equal-priority elements.\n\nAdvantages: O(1) peek at min/max, O(log n) insert and extract, memory-efficient array layout, handles dynamic streams well.\nDisadvantages: O(n) arbitrary remove/contains, no stable ordering among equal elements, not thread-safe, no efficient decrease-key without a custom indexed heap.",
        whenToUse: "Use for top-K queries, scheduling by priority, Dijkstra, and stream processing where repeated best-element extraction is needed.",
        why: "Binary heap keeps root as best candidate (min by default), making push/pop logarithmic.",
        complexity: { offer: "O(log n)", poll: "O(log n)", peek: "O(1)" },
        tradeoffs:
          "Java PriorityQueue is a MinHeap by default. For MaxHeap use Comparator.reverseOrder(). No indexed random access; arbitrary remove/contains are O(n).",
        codeVariants: [
          { title: "PriorityQueue basics", code: JAVA_SNIPPETS.priorityQueue },
          { title: "MinHeap (default)", code: JAVA_SNIPPETS.priorityQueueMinHeap },
          { title: "MaxHeap (reverse comparator)", code: JAVA_SNIPPETS.priorityQueueMaxHeap },
          { title: "MinHeap from scratch", code: JAVA_SNIPPETS.minHeapFromScratch },
          { title: "MaxHeap from scratch", code: JAVA_SNIPPETS.maxHeapFromScratch },
        ],
        javaCode: JAVA_SNIPPETS.priorityQueue,
        color: "#8b5cf6",
      },
    },
    {
      label: "Trie",
      result: {
        name: "Trie",
        pkg: "Custom trie (prefix tree)",
        description: "A Trie (prefix tree) is an n-ary tree where each node represents one character of a string. Every path from root to a marked node spells a complete word. Insertion and lookup cost O(L) where L is the string length — completely independent of how many words are stored. This makes Tries far superior to HashSets for prefix matching, autocomplete, and character-by-character search on large dictionaries.\n\nAdvantages: O(L) insert/search/prefix-check regardless of total word count, shared prefix storage saves memory for overlapping words, natural autocomplete and longest-prefix-match support.\nDisadvantages: High memory for sparse alphabets (26-way branching per node), more complex to implement than a HashSet, large constant per node.",
        whenToUse: "Use for dictionary prefix queries, autocomplete, and word search problems.",
        why: "Character-by-character branching gives fast prefix operations independent of total words.",
        complexity: { insert: "O(L)", search: "O(L)", startsWith: "O(L)", space: "O(total characters)" },
        tradeoffs: "Memory-heavy for sparse alphabets; use map-based nodes if alphabet is large.",
        javaCode: JAVA_SNIPPETS.trie,
        color: "#0ea5e9",
      },
    },
    {
      label: "Red-Black Tree (via TreeMap/TreeSet)",
      result: {
        name: "Red-Black Tree (via TreeMap/TreeSet)",
        pkg: "java.util.TreeMap / java.util.TreeSet",
        description: "A Red-Black Tree is a self-balancing Binary Search Tree that colors every node red or black and enforces five invariants (root is black, red nodes have black children, equal black-height on every root-to-null path, etc.) to guarantee the tree height stays within 2× the theoretical minimum. Rotations and recolorings after insert/delete restore balance in O(log n). Java uses RBT internally for TreeMap and TreeSet.\n\nAdvantages: Guaranteed O(log n) for all operations (no degenerate cases), BST order enables NavigableMap/Set API, efficient in-order traversal.\nDisadvantages: More complex to implement from scratch than AVL trees, higher constant factors than hash tables for plain lookup, each node carries extra color/pointer memory.",
        whenToUse:
          "Use when order-aware operations are mandatory: floor/ceiling/lower/higher, rank-like boundary queries, sorted iteration, and range slices/submaps. Typical use cases: interval scheduling, nearest neighbor on sorted keys, time-ordered event indexing, sweep-line problems, leaderboards with ordered keys, and any dynamic set/map where both updates and ordered queries are frequent.",
        why: "Red-Black Tree maintains near-perfect balance through recoloring and rotations after updates, guaranteeing O(log n) height while preserving BST order semantics.",
        complexity: { put: "O(log n)", get: "O(log n)", remove: "O(log n)", rangeOps: "O(log n) + output" },
        tradeoffs:
          "Higher constants and memory than hash tables; if you only need exact lookup, HashMap/HashSet is usually faster. But when sorted order and predecessor/successor queries are required, RBT-backed structures are one of the best general-purpose choices.",
        codeVariants: [
          { title: "Java TreeMap/TreeSet usage", code: JAVA_SNIPPETS.redBlackTreeConcept },
          { title: "How Red-Black Tree works", code: JAVA_SNIPPETS.redBlackTreeHowItWorks },
          { title: "From-scratch Java implementation", code: JAVA_SNIPPETS.redBlackTreeFromScratch },
        ],
        javaCode: JAVA_SNIPPETS.redBlackTreeConcept,
        color: "#ef4444",
      },
    },
    {
      label: "Union-Find (Disjoint Set Union)",
      result: {
        name: "Union-Find (Disjoint Set Union)",
        pkg: "Custom DSU",
        description: "Union-Find (DSU) partitions n elements into disjoint sets using two arrays: parent[] and size[]. find(x) walks parent pointers to the root and applies path compression (flattening the path for future calls). union(x, y) links the smaller tree under the larger (union by size/rank). Together these optimizations give an amortized near-O(1) per operation (technically O(α(n)), where α is the inverse Ackermann function — effectively constant for all practical n).\n\nAdvantages: Near-O(1) amortized find/union, trivial implementation, ideal for offline connectivity queries and Kruskal's MST.\nDisadvantages: No efficient path queries between nodes, does not support edge deletion (offline only), cannot reconstruct the actual path between components.",
        whenToUse: "Use for connected components, cycle detection in undirected graphs, Kruskal MST.",
        why: "Path compression + union by size keeps operations near constant amortized.",
        complexity: { findUnion: "O(alpha(n)) amortized", space: "O(n)" },
        tradeoffs: "Great for connectivity but not for shortest path or path reconstruction.",
        javaCode: JAVA_SNIPPETS.unionFind,
        color: "#22c55e",
      },
    },
    {
      label: "Fenwick Tree (BIT)",
      result: {
        name: "Fenwick Tree (BIT)",
        pkg: "Custom BIT",
        description: "A Fenwick Tree (Binary Indexed Tree) stores partial sums in a 1-indexed array where each cell i is responsible for a range of length lowbit(i) = i & (-i). A point update propagates changes up by repeatedly adding lowbit(i), and a prefix sum query accumulates by repeatedly stripping lowbit(i) — both operations touch at most O(log n) cells. The result is a compact, cache-friendly structure that replaces a prefix-sum array when the underlying data changes frequently.\n\nAdvantages: O(log n) point update and prefix sum, minimal code (~15 lines), only O(n) extra space, very cache-friendly.\nDisadvantages: Only supports invertible aggregates (sum, XOR) — not min/max without tricks, 1-indexed API is error-prone, harder to extend to range updates without extra BITs.",
        whenToUse: "Use when you need many updates and prefix/range sums on numeric arrays.",
        why: "Bitwise index jumps compress segment coverage into logarithmic updates/queries.",
        complexity: { update: "O(log n)", prefixSum: "O(log n)", rangeSum: "O(log n)", space: "O(n)" },
        tradeoffs: "Less expressive than segment tree for min/max or custom merge operations.",
        javaCode: JAVA_SNIPPETS.fenwick,
        color: "#8b5cf6",
      },
    },
    {
      label: "Segment Tree",
      result: {
        name: "Segment Tree",
        pkg: "Custom Range Query Tree",
        description: "A Segment Tree is a binary tree built over an array where each node stores the aggregate (sum, min, max, GCD, …) for a contiguous range. The root covers the whole array; each internal node splits its range in half. A point update touches O(log n) nodes along the root-to-leaf path, and a range query merges O(log n) pre-computed segments. Lazy propagation extends this to range updates (e.g., add 5 to all elements in [l, r]) in O(log n).\n\nAdvantages: O(log n) point update and range query for any associative function, supports lazy range updates, highly flexible (custom merge functions).\nDisadvantages: ~4n array space required, ~50-80 lines of code, boundary and lazy-push bugs are common, higher constant than Fenwick Tree for pure prefix sums.",
        whenToUse: "Use when you need fast range queries (sum/min/max) and updates on mutable arrays.",
        why: "Hierarchical interval decomposition supports query/update in logarithmic time.",
        complexity: { build: "O(n)", pointUpdate: "O(log n)", rangeQuery: "O(log n)", rangeUpdateLazy: "O(log n)" },
        tradeoffs: "More code and memory (about 4n); easy to make boundary/lazy bugs.",
        javaCode: JAVA_SNIPPETS.segmentTree,
        color: "#7c3aed",
      },
    },
    {
      label: "Sparse Table",
      result: {
        name: "Sparse Table",
        pkg: "Static Range Query Structure",
        description: "A Sparse Table precomputes answers for all ranges whose length is a power of two. Table[i][j] stores the aggregate for the range starting at index j with length 2^i. Building takes O(n log n) time and space. For idempotent functions (min, max, GCD — where combining overlapping results is safe), any range query [l, r] is answered in O(1) by choosing two overlapping power-of-two blocks that together cover [l, r]. For non-idempotent functions (sum) the query degrades to O(log n).\n\nAdvantages: O(1) range-min/max queries after O(n log n) preprocessing, simple implementation, great for static read-heavy workloads.\nDisadvantages: Cannot handle updates — the table must be fully rebuilt after any change, O(n log n) memory is worse than Segment Tree's O(n), only true O(1) for idempotent aggregates.",
        whenToUse: "Use when array is immutable and you need many idempotent range queries (min/max/gcd).",
        why: "Precomputed powers-of-two blocks answer queries in O(1) for RMQ-style operations.",
        complexity: { preprocess: "O(n log n)", queryRMQ: "O(1)", update: "Not supported efficiently" },
        tradeoffs: "Not suitable for frequent updates; rebuild required after changes.",
        javaCode: JAVA_SNIPPETS.sparseTable,
        color: "#6d28d9",
      },
    },
    {
      label: "Deque / Monotonic Queue",
      result: {
        name: "Deque / Monotonic Queue",
        pkg: "ArrayDeque Pattern",
        description: "A Deque (double-ended queue) supports O(1) add/remove at both ends. The Monotonic Queue pattern uses an ArrayDeque of indices maintained in strictly increasing or decreasing order of their corresponding values. Before adding a new index, all indices at the back whose values are worse (≥ for max-window, ≤ for min-window) are popped — they can never be the answer. The front is popped when it slides out of the window. Every index is pushed and popped at most once, giving O(n) total for an n-element scan.\n\nAdvantages: O(1) per element amortized, solves sliding-window min/max in O(n) vs O(n log n) with a heap, also accelerates certain DP transitions.\nDisadvantages: Requires careful index management, more subtle to implement correctly than a simple heap, only directly applicable when the best candidate can be maintained monotonically.",
        whenToUse: "Use for sliding-window max/min and constrained DP where stale/weak candidates must be evicted.",
        why: "Deque maintains useful candidates in monotonic order with O(1) amortized updates.",
        complexity: { pushPop: "O(1) amortized", windowQuery: "O(1)", totalForN: "O(n)" },
        tradeoffs: "Must store indices and expire out-of-window elements correctly.",
        javaCode: JAVA_SNIPPETS.swMonotonicDeque,
        color: "#0f766e",
      },
    },
    {
      label: "Ordered Multiset via TreeMap",
      result: {
        name: "Ordered Multiset (TreeMap)",
        pkg: "java.util.TreeMap + counts",
        description: "Java has no built-in multiset, but TreeMap<Integer, Integer> (value → count) emulates one. Adding an element increments its count; removing decrements and deletes the key when the count hits zero. Because the underlying structure is a Red-Black Tree, floor/ceiling/lower/higher and sorted iteration all work in O(log n) even with duplicates. This pattern is common in sliding-window problems that need the kth-smallest or ordered range access.\n\nAdvantages: Handles duplicates with O(log n) ordered operations, no external library needed, supports floor/ceiling on values with counts.\nDisadvantages: More verbose than C++ std::multiset, no direct rank/select (kth element requires manual prefix-sum tracking), slightly higher constant than a raw TreeSet.",
        whenToUse: "Use when duplicates matter and you need ordered operations like floor/ceiling/range by value.",
        why: "TreeMap keeps sorted keys while counts emulate multiset behavior.",
        complexity: { addRemove: "O(log n)", floorCeil: "O(log n)", iterateSorted: "O(n)" },
        tradeoffs: "More verbose than C++ multiset; no direct order-statistics rank/select.",
        javaCode: JAVA_SNIPPETS.orderedMultisetTreeMap,
        color: "#be123c",
      },
    },
    {
      label: "Graph Representations",
      result: {
        name: "Graph Representations",
        pkg: "Adjacency List / Matrix / Edge List",
        description: "Graphs can be stored in three primary ways. Adjacency List (List<List<Integer>> or Map<Node, List<Node>>): each vertex holds only its own neighbors — O(V+E) space, O(deg(v)) neighbor iteration, best for sparse graphs. Adjacency Matrix (int[V][V]): 2D boolean/weight matrix — O(V²) space, O(1) edge lookup, best for dense graphs or when frequent edge-existence checks dominate. Edge List (int[][] edges): a flat list of (u, v, weight) triples — O(E) space, ideal for sorting edges by weight (Kruskal) or DSU workflows.\n\nAdvantages: Each representation optimizes a different access pattern; choosing correctly reduces both memory and algorithm runtime.\nDisadvantages: Adjacency list — O(deg) edge check; matrix — O(V²) memory even for sparse graphs; edge list — no fast neighbor iteration.",
        whenToUse: "Use adjacency list for sparse graphs, matrix for dense/small graphs, edge list for sorting/DSU workflows.",
        why: "Choosing the right representation directly affects memory, traversal speed, and algorithm fit.",
        complexity: { adjListSpace: "O(V+E)", matrixSpace: "O(V^2)", neighborIterAdjList: "O(deg(v))", edgeLookupMatrix: "O(1)" },
        tradeoffs: "Wrong representation can make good algorithm appear slow.",
        javaCode: JAVA_SNIPPETS.graphRepresentations,
        color: "#0ea5e9",
      },
    },
    {
      label: "Two Heaps (Median DS)",
      result: {
        name: "Two Heaps (Median DS)",
        pkg: "Dual PriorityQueue",
        description: "The Two Heaps pattern maintains two heaps: a max-heap for the lower half of numbers and a min-heap for the upper half. After each insertion the sizes are rebalanced so they differ by at most 1. The median is then either the max-heap root (odd total count) or the average of both roots (even total count) — both O(1) reads. Insertions cost O(log n) per element.\n\nAdvantages: O(1) median query, O(log n) insert, handles unbounded online streams without storing all elements sorted.\nDisadvantages: Deletions require lazy removal with a HashMap (non-trivial), the strict rebalancing invariant is easy to break, two heaps consume 2× the memory of a single sorted structure.",
        whenToUse: "Use when numbers stream in and median must be queried repeatedly online.",
        why: "Max-heap for lower half + min-heap for upper half keeps middle split balanced.",
        complexity: { addNum: "O(log n)", findMedian: "O(1)", space: "O(n)" },
        tradeoffs: "Need strict rebalance invariants; deletions are non-trivial without lazy removal maps.",
        javaCode: JAVA_SNIPPETS.twoHeapsMedian,
        color: "#9333ea",
      },
    },
    {
      label: "ConcurrentHashMap",
      result: {
        name: "ConcurrentHashMap",
        pkg: "java.util.concurrent.ConcurrentHashMap",
        description: "ConcurrentHashMap is a thread-safe hash map designed for high concurrency. Unlike the old Hashtable (which synchronized every method on a single lock), ConcurrentHashMap uses fine-grained bucket-level locking (CAS + synchronized on individual bins in Java 8+). Reads are non-blocking; writes lock only the affected bucket. This allows many threads to read and write different buckets simultaneously without contention.\n\nAdvantages: High-throughput concurrent reads (non-blocking), fine-grained locking for writes, atomic compute/merge/putIfAbsent helpers, no external synchronization needed.\nDisadvantages: Does not allow null keys or values, aggregate operations (size, isEmpty) are not strongly consistent under concurrent updates, overkill for single-threaded code.",
        whenToUse: "Use when many threads read/write shared key-value state.",
        why: "Designed for high concurrency with non-blocking reads and scalable updates.",
        complexity: { get: "O(1) avg", put: "O(1) avg", compute: "O(1) avg" },
        tradeoffs: "More complexity than HashMap; avoid when single-threaded.",
        javaCode: JAVA_SNIPPETS.concurrentHashMap,
        color: "#ec4899",
      },
    },
  ],
};
